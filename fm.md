# 풋고추FC 매치 엔진 개발 가이드 (V3 기준)

이 문서는 풋고추FC의 실제 데이터를 바탕으로 구동되는 6대6 풋살 시뮬레이터의 뼈대와 핵심 로직을 설명합니다. 다른 AI나 개발자와 협업할 때 이 문서를 먼저 제시하면 기존 코드를 안전하게 유지하면서 기능을 확장할 수 있습니다.

## 1. 프로젝트 개요
* **개발 언어:** HTML5 Canvas와 JavaScript
* **목적:** 팀원들의 실제 능력치 데이터를 활용한 2D 바둑알(Dot) 풋살 매치 시뮬레이션
* **실행 방식:** 웹 브라우저 단일 HTML 파일 구동 (별도 서버 구축 불필요)

## 2. 데이터 연동 구조 (Google Apps Script)
기존 웹 크롤링 방식의 CORS(보안) 문제를 해결하기 위해, Google Apps Script(GAS)를 백엔드 서버처럼 활용하여 데이터를 연동합니다. 

* **API 엔드포인트 URL:** `https://script.google.com/macros/s/AKfycbzdLjnfQEFuF0c6sH75D7gLyaPUg4LJWSDJLAym5kWKucot-CbY-QEkrDI7DnaxRYpe/exec`
* **통신 방식:** `fetch` API를 이용한 POST 요청

### 핵심 API Action 코드
서버에 특정 데이터를 요청할 때 사용하는 암호(Action)입니다.

| Action 명칭 | 용도 | 파라미터 예시 |
| :--- | :--- | :--- |
| `getMemberInfo` | 개별 선수 능력치 호출 | `{ action: 'getMemberInfo', memberName: '강승지' }` |
| `getAvgMemberInfo` | 팀 전체 평균 능력치 호출 | `{ action: 'getAvgMemberInfo' }` |

## 3. 매치 엔진 핵심 로직
게임은 60FPS로 무한히 화면을 다시 그리는 `requestAnimationFrame` 루프 안에서 돌아갑니다.

* **상태 머신 (gameState):** 게임의 흐름을 제어하는 가장 중요한 변수입니다. `PLAYING`(진행 중), `GOAL`(득점), `FOUL`(반칙) 등의 상태를 구분하여 바둑알의 행동을 통제합니다.
* **좌표 기반 물리 엔진:** 800x400 캔버스를 기준으로 공의 좌표(x, y)가 벽이나 특정 영역에 닿으면 아웃(outOfBounds) 또는 득점(goalScored)으로 판정합니다.
* **능력치 반영:** 스피드(13~16) 스탯을 바둑알이 이동하는 속도로 변환하여 움직임에 직접 반영합니다.

## 4. 향후 AI에게 개발을 요청할 때 (프롬프트 팁)
새로운 AI에게 코드를 수정해달라고 할 때는 기존 엔진의 틀이 깨지지 않도록 아래의 **요청 템플릿**을 활용하세요.

> **AI 요청 템플릿 예시:**
> "나는 HTML5 Canvas와 JS로 만든 풋살 매치 엔진을 가지고 있어. 현재 Google Apps Script를 통해 선수 데이터를 실시간으로 받아오고, `gameState`로 게임 상태를 관리하는 구조야. 
> 
> **[요청 사항]**
> 기존 HTML 구조와 API 연동 로직은 절대 건드리지 말고, `updateGame()` 함수 내부에 선수들이 공을 가진 상태일 때 주변 동료에게 패스하는 로직을 추가해 줘. 
> 이때 선수의 '패스' 스탯과 '시야' 스탯을 확률 계산식에 넣어서 성공/실패 여부를 결정하게 만들어 줘. (예: `if (Math.random() * 20 < player.passStat)`)"

## 5. 다음 개발 우선순위 (To-Do List)
엔진의 기초가 완성되었으므로 다음 기능들을 순차적으로 도입해야 합니다.

* **패스 및 슛 인공지능:** 공을 무작정 쫓아가는 것이 아니라, 같은 팀 진영(`p.team === 'A'`)일 때의 패스 및 슈팅 로직 구현.
* **포메이션 복귀 로직:** 2-2-1, 1-3-1 등 포메이션에 따라 피보, 픽소, 아라의 기본 복귀 위치(Home Position)를 계산하는 공식 추가.
* **충돌 및 몸싸움:** 바둑알끼리 겹쳤을 때 '몸싸움' 스탯을 비교하여 공의 소유권을 가져오는 로직 추가.
